git add internal/api/handlers/nomba_webhook.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add internal/api/handlers/system_health.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add internal/api/handlers/invoices.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add internal/api/router.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add internal/payment/nomba_client.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add internal/billing/handlers.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add cmd/worker/main.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add internal/reconciliation/service.go
git commit -m "feat: nightly reconciliation — fetches Nomba sub-account transactions, matches against ledger by merchantTxRef, flags missing and mismatched entries, persists run summary"

git add internal/reconciliation/handler.go
git commit -m "feat: nightly reconciliation — fetches Nomba sub-account transactions, matches against ledger by merchantTxRef, flags missing and mismatched entries, persists run summary"

git add internal/reconciliation/scheduler.go
git commit -m "feat: nightly reconciliation — fetches Nomba sub-account transactions, matches against ledger by merchantTxRef, flags missing and mismatched entries, persists run summary"

git add internal/payment/client.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add internal/payment/mock.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add cmd/api/main.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add internal/domain/models.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add internal/domain/repositories.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add internal/postgres/subscription_repo.go
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add db/queries/subscriptions.sql
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add db/queries/reconciliation.sql
git commit -m "feat: nightly reconciliation — fetches Nomba sub-account transactions, matches against ledger by merchantTxRef, flags missing and mismatched entries, persists run summary"

git add db/queries/
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add db/migrations/000007_add_subscription_token_key.up.sql
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add db/migrations/000007_add_subscription_token_key.down.sql
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add db/migrations/000008_reconciliation.up.sql
git commit -m "feat: nightly reconciliation — fetches Nomba sub-account transactions, matches against ledger by merchantTxRef, flags missing and mismatched entries, persists run summary"

git add db/migrations/000008_reconciliation.down.sql
git commit -m "feat: nightly reconciliation — fetches Nomba sub-account transactions, matches against ledger by merchantTxRef, flags missing and mismatched entries, persists run summary"

git add frontend/app/dashboard/invoices/page.tsx
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add frontend/lib/api.ts
git commit -m "feat: Nomba integration complete — checkout, tokenised card, mandates, webhook idempotency, invoice handler, system health endpoint"

git add internal/billing/abandoned_checkout.go
git commit -m "fix: handle all checkout edge cases — abandoned checkout job, payment_failed webhook handler, regenerate checkout endpoint, trial verification charge 100 kobo, TokenKey mapped in subFromRow, invoice+ledger order fixed"

git add internal/billing/abandoned_scheduler.go
git commit -m "fix: handle all checkout edge cases — abandoned checkout job, payment_failed webhook handler, regenerate checkout endpoint, trial verification charge 100 kobo, TokenKey mapped in subFromRow, invoice+ledger order fixed"

git add internal/api/handlers/refund.go
git commit -m "feat: refund system — automatic ₦50 trial verification refund, manual refund endpoint POST /v1/subscriptions/{id}/refund, REFUND ledger entry, Platform API refund endpoint"

git add internal/api/handlers/ledger.go
git commit -m "fix: parseDateRange sets to=end of day so same-day ledger entries are included in summary queries"

git add internal/subscription/state_machine.go
git commit -m "feat: PENDING_PAYMENT state — subscriptions start pending, activate only after Nomba payment_success webhook, invoice and ledger entry created on activation, trial verification ₦50 refunded automatically, payment_failed moves pending to PAST_DUE"

git add internal/subscription/state_machine_test.go
git commit -m "feat: PENDING_PAYMENT state — subscriptions start pending, activate only after Nomba payment_success webhook, invoice and ledger entry created on activation, trial verification ₦50 refunded automatically, payment_failed moves pending to PAST_DUE"

git push

tori_live_a5eac054adef09a31a7d823d6e71b1245deffa7eb5c8a51c765e7678e9d578c0
